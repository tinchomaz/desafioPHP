La base de datos esta configurada con las especificaciones de mi computadora (usuario root y sin contraseña)

Adjunto la misma base exportada que use.Estara en la carpeta database/exportado.

En caso de que no funcione adjunte el script que use para crear la base de datos